server:
  port: 8081
  servlet:
    context-path: /api

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/doctor_appointment"
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
